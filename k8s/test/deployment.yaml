apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-notification-server
  labels:
    tier: backend
  namespace: thothee-test
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-notification-server
      tier: backend
  template:
    metadata:
      labels:
        app: app-notification-server
        tier: backend
    spec:
      containers:
        - name: app-notification-server
          image: eu.gcr.io/thothee/app-notification-server-k8s:v2
          env:
            - name: JAVA_OPTS
              value: '-Xms256m -Xmx512m -Dspring.cloud.config.label=test -Dspring.cloud.config.uri=http://admin:admin@utils-registry-server.thothee-test.svc.cluster.local:8888/config -Djhapy.registry.password=admin'
            - name: SERVER_NAME
              value: '{{.Node.Hostname}}-{{.Node.ID}}'
            - name: SPRING_PROFILES_ACTIVE
              value: k8s,k8s-test,swagger
          resources:
            requests:
              memory: '256Mi'
              cpu: '250m'
            limits:
              memory: '512Mi'
              cpu: '1'
          ports:
            - name: http
              containerPort: 9003
            - name: management
              containerPort: 9103
          readinessProbe:
            httpGet:
              path: /management/health/readiness
              port: management
            initialDelaySeconds: 30
            periodSeconds: 15
            failureThreshold: 6
          livenessProbe:
            httpGet:
              path: /management/health/liveness
              port: management
            initialDelaySeconds: 120