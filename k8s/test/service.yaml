apiVersion: v1
kind: Service
metadata:
  name: app-notification-server
  labels:
    tier: backend
  namespace: thothee-test
spec:
  type: NodePort
  selector:
    app: app-notification-server
  ports:
    - name: http
      port: 9003
      targetPort: 9003
